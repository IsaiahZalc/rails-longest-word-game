<div class="container">
  <h1>NEW GAME 🎮</h1>
  <div class="d-flex">
    <% @array_letters.each do |letter| %>
     <div class="btn btn-primary m-2"><%= letter %></div>
    <% end %>
  </div>
  <p>What is the longest word you can find?</p>
  <form action="score" method="post" data-turbo="false">
    <input class="text-center" class="form-control form-control-lg" type="text" name="word" placeholder="Your Word">
    <input button type="submit" class="btn btn-primary" value="Play"></button>
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <%= hidden_field_tag :letters, @array_letters %>
  </form>
</div>
